{
  "name": "totui-web-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check kanban.js && node --check clipboard.js && prettier --check *.js *.css *.html tests/*.js",
    "test": "playwright test",
    "format": "prettier --write *.js *.css *.html tests/*.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "prettier": "^3.9.6"
  }
}
