{
  "name": "totui-web-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && prettier --check app.js style.css index.html playwright.config.js tests/*.js",
    "test": "playwright test",
    "format": "prettier --write app.js style.css index.html playwright.config.js tests/*.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "prettier": "^3.9.6"
  }
}
