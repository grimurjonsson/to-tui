---
phase: 04-claude-code-plugin-configuration
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README documents correct Claude Code plugin installation commands"
    - "README explains plugin marketplace add command"
    - "README includes binary installation step after plugin install"
  artifacts:
    - path: "README.md"
      provides: "Updated installation documentation"
      contains: "/plugin marketplace add"
  key_links:
    - from: "README.md"
      to: "scripts/install-binary.sh"
      via: "installation instructions"
      pattern: "install-binary\\.sh"
---

<objective>
Update README with correct Claude Code plugin installation instructions.

Purpose: Provide accurate, up-to-date installation instructions that follow current Anthropic documentation patterns for Claude Code plugins.

Output: README.md with correct plugin installation commands and workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-claude-code-plugin-configuration/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP Server installation section in README</name>
  <files>README.md</files>
  <action>
Update the "### MCP Server (for LLMs)" section with correct Claude Code plugin commands.

Replace the current installation instructions with the correct commands from Anthropic documentation:

```markdown
### MCP Server (for LLMs)

The MCP server allows AI assistants like Claude to manage your todos.

#### Installation as Claude Code Plugin

**Option 1: Plugin Marketplace (Recommended)**

1. Open Claude Code
2. Add the marketplace:
   ```
   /plugin marketplace add grimurjonsson/to-tui
   ```
3. Install the plugin:
   ```
   /plugin install totui-mcp@grimurjonsson/to-tui
   ```
4. Download the pre-built binary (one-time setup):
   ```bash
   cd ~/.claude/plugins/cache/grimurjonsson-to-tui/totui-mcp
   bash scripts/install-binary.sh
   ```
5. Restart Claude Code

The MCP server tools (list_todos, create_todo, etc.) will now be available.

**Option 2: Direct MCP Configuration**

Add totui-mcp directly to your Claude Code configuration:

```bash
# User-scoped (available in all projects)
claude mcp add --transport stdio --scope user totui-mcp -- /path/to/totui-mcp

# Project-scoped (creates .mcp.json in project)
claude mcp add --transport stdio --scope project totui-mcp -- /path/to/totui-mcp
```

Verify installation:
```bash
claude mcp list
```

Or check in Claude Code:
```
/mcp
```
```

Keep the rest of the MCP section (Local Development Setup, Manual MCP Server Setup, pre-built binaries list) but update paths if needed.

Important: Remove references to the outdated `/plugin` UI steps. The correct method is using slash commands as shown above.
  </action>
  <verify>README contains "/plugin marketplace add grimurjonsson/to-tui" and "/plugin install totui-mcp@grimurjonsson/to-tui"</verify>
  <done>README MCP installation section uses correct Claude Code plugin commands</done>
</task>

<task type="auto">
  <name>Task 2: Add alternative user-scoped MCP setup instructions</name>
  <files>README.md</files>
  <action>
Ensure the README includes the `claude mcp add` alternative for users who don't want to use the plugin system.

This should already be covered in Task 1, but verify the section includes:
1. `claude mcp add --scope user` command example
2. `claude mcp list` for verification
3. `/mcp` command in Claude Code to check status

Also update the "Manual MCP Server Setup" section for non-Claude Code tools (Claude Desktop, OpenCode) with the correct JSON format:

```json
{
  "totui-mcp": {
    "command": "/path/to/totui-mcp",
    "args": []
  }
}
```

Note: The current README has a different format with "mcp" wrapper and "enabled" field. Update to match the simpler format used in .mcp.json files.
  </action>
  <verify>README contains "claude mcp add" command and correct JSON format for manual setup</verify>
  <done>README includes both plugin and direct MCP configuration options</done>
</task>

</tasks>

<verification>
1. README contains `/plugin marketplace add grimurjonsson/to-tui`
2. README contains `/plugin install totui-mcp@grimurjonsson/to-tui`
3. README contains `claude mcp add` alternative
4. README contains correct path to install-binary.sh
5. Manual setup JSON format matches .mcp.json structure
</verification>

<success_criteria>
- Plugin installation uses correct slash commands from Anthropic docs
- Both plugin and direct MCP add methods documented
- Binary installation step clearly explained
- Manual setup for non-Claude Code tools uses correct JSON format
</success_criteria>

<output>
After completion, create `.planning/phases/04-claude-code-plugin-configuration/04-03-SUMMARY.md`
</output>
